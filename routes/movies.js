const movieRouter = require('express').Router();
// const { getMe } = require('../controllers/users');

// movieRouter.get('/me', getMe);
// movieRouter.patch('/me', getMe);

module.exports = { movieRouter };
